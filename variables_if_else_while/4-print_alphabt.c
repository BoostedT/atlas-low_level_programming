#include <stdio.h>
/**
 * main - Print all the letters except q and e
 *
 * Return: 0
 */
int main(void)
{
while (c <= 'z')
{
if (c != 'q' && c != 'e')
{
putchar(c);
}
c++;
}
putchar'\n');
return (0);
}
