#include <stdio.h>
/**
 * main - prints all the numbers of base 16 in lowercase followed by a new line
 *
 * Return: 0
 */
int main(void)
{
int i;
for (int i = '0'; i <= '9'; i++)
{
putchar(i);
}
for (int l = 'a'; i <= 'f' i++)
{
putchar(l);
}
putchar('\n');
return (0);
}
